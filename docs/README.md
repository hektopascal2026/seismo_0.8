# Database schema

**`db-schema.sql`** is the consolidated base schema applied by migration 001. The numbered migrations in `src/Migration/` bring existing databases forward.

Run **`php migrate.php`** (mothership only) after deploy.
